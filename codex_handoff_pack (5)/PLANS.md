# Plans (High-level)

This repository is implemented via Codex CLI tasks defined in `tasks/tasks.json`.

Milestones:
- **P0**: runnable baseline for CVD steady forward simulation + robust TCRSK core + reporting + tests
- **P1**: DOE framework + additional models + improved diagnostics + JAX engine skeleton (optional)
- **P2**: Data assimilation/optimization package + ALD phases/state models + ClearML integration + advanced IO

See:
- `plans/EXECPLAN.md` for detailed execution plan
- `tasks/TASKS.md` for task list and acceptance criteria
