以下は、提示した仕様（回転ディスク寄り single‑wafer、CVD優先だがALDも対応、CFD直上面入力＋TC結合＋縮約表面反応運動論、多次数、CPU/GPU、DOE、同化、Hydra/YAML、レジストリ、迷子にならない出力）を **実務で回したときに実際に起きやすい課題**を、物理・装置・数値計算・運用・データ同化/MLの視点で洗い出し、**検出方法（どう気づくか）と対応策（どう潰すか）**まで含めて整理したものです。

---

# 1. 実務利用で最初に詰まりやすい「5大課題」

まず全体像として、実務で“最初に効くのに壊れやすい”のはだいたい次の5つです。

1. **CFD参照面（z_ref）の意味のズレ**

   * 参照面濃度をバルク濃度だと思い込むと、境界層抵抗が二重計上/無視されて、次数・速度定数が漂流する。

2. **回転ディスク想定（軸対称・相関式）と実機のズレ**

   * 回転ディスク相関（Levich型など）は“理想化”なので、装置固有の流れ（シャワーヘッド・偏流・温度非一様）で破綻することがある。

3. **反応次数・阻害・飽和の“同定性の悪さ”**

   * 膜厚2Dマップだけだと、パラメータが相互補償して「当たるが外挿が壊れる」になりやすい。

4. **数値の頑健性：rootが単根でなくなる/硬いODE/非滑らか制約**

   * 負次数・強い分母・複数チャネル（dep−etch）を入れると、局所方程式が非単調になり得る。

5. **DOEが増えたときの“実行戦略と出力管理”**

   * 10〜1000ケースを回すと、GPUの有効性・ログ/出力の迷子・再現性が問題になる。

以下、これを「課題→影響→検出→対策」として細かく分解します。

---

# 2. 課題一覧（影響・検出・対策・優先度つき）

> 優先度：
> **P0**＝最初に必ず潰す（放置すると全体が壊れる）
> **P1**＝導入初期で高確率に効く
> **P2**＝スケール/高度化で効く

| 観点  | 課題（実務で起きること）                   | 影響                     | どう検出するか（自動診断推奨）                      | 対策（設計/運用）                                                                 | 優先 |
| --- | ------------------------------ | ---------------------- | ------------------------------------ | ------------------------------------------------------------------------- | -- |
| 入力  | CFDの参照面 z_ref が境界層内/外で解釈が変わる   | kmや次数がz_refに依存し、外挿で壊れる | z_ref を変えたときの膜厚差、Cs/Cref、Da proxyの変化 | z_refは“参照面”として扱い、km側に吸収自由度。z_ref感度DOEを標準化。将来multi‑z入力に拡張                  | P0 |
| 入力  | CFD→ウェハ格子への補間・座標ずれ（中心ズレ/回転ズレ）  | 2D膜厚残差が系統誤差に見える        | 位置合わせ最適化前後の残差、中心/エッジ差の偏り             | MeasurementAdapterを標準化（中心/回転/スケール補正、マスク）                                  | P0 |
| 物理  | 回転ディスク相関が装置固有流れに合わない           | km推定が誤り→膜厚ムラ予測が歪む      | 推定kmが非現実値、Ck（補正係数）が条件で漂流             | kmモデルを複数用意（停滞膜/Levich/CFD派生）。Ckを同化対象にし、条件依存ならCk(r)基底で拡張                   | P0 |
| 物理  | 反応種A/Bの“まとめ方”が条件で変わる           | 同じ条件でも再現性が落ちる          | 同化した次数/係数が条件群で不整合                    | まずはA/Bの定義を固定。必要なら「有効濃度重み」をパラメータ化（w_i）し、同化や回帰で推定                           | P1 |
| 物理  | 飽和/阻害（分母形）を入れずに次数で吸収           | 次数が漂流、外挿で破綻            | 条件レンジ外で急激に外れる、n_appが異常               | 最初からSAT/INHを“選べる”状態にしてモデル比較。阻害候補を早期に入れる                                   | P0 |
| 物理  | dep−etch−loss（多チャネル）でネットが小さい領域 | 同化が不安定（パラメータが暴れる）      | パラメータ発散、損失関数が平坦/多峰性                  | 切り分け実験（depのみ/etchのみ）を推奨。正則化・事前分布を強化。チャネル数は段階的に増やす                         | P1 |
| 数値  | 1D rootの前提（単調性・単根）が崩れる         | bisectionが誤った根/収束失敗    | F(R)の符号変化が複数回、反復が収束しない               | **単調性チェック**を実装（F’符号/サンプル点）。崩れる場合は(1)モデル制約、(2)根探索を区間分割、(3)多変数ソルバへフォールバック   | P0 |
| 数値  | クリップ（clip）で非負性を守ると同化で勾配が壊れる    | 勾配が0/不連続で最適化停止         | 勾配ノルムが0、LBFGSが停滞                     | 同化エンジンでは softplus/sigmoid等の滑らか拘束に切替（YAMLで選択）                              | P1 |
| 数値  | stiff ODE（ALD/改質）で時間積分が不安定     | 被覆率が振動/負になる、計算が遅い      | θが[0,1]外、step数爆増                     | 解析更新優先、次にimplicit（BDF/Radau）。dt自動調整。状態変数を最小に保つ                            | P1 |
| 計算  | 小点数単発でGPUが遅い（JIT/転送が支配）        | 実行が遅く不満が出る             | 1ケースがGPUでCPUより遅い                     | run_strategy=autoで単発はCPU、DOEはGPUバッチへ。JITキャッシュを使う                          | P1 |
| 計算  | float64がGPUで遅い/使えない環境          | 精度不足or速度不足             | float32/64で結果差、GPU性能低下               | precision切替をYAMLで。必要箇所のみx64、他はx32の混合戦略（段階導入）                              | P2 |
| DOE | 1000条件×2Dで出力が爆発（迷子・容量）         | 解析不能、運用破綻              | runsディレクトリ肥大、見たい結果に辿り着けない            | ケースディレクトリを掘らず、zarrにcase次元で集約。index.htmlに要約＋検索機能                           | P0 |
| 運用  | Hydra configが増殖し迷子             | 誤設定・再現不能               | config数が膨れ、defaultsが複雑化              | Structured config＋最小group。モデル選択はname+paramsで統一。resolved configはrunに1つのみ保存 | P0 |
| V&V | 物理診断が無く「当たった理由」が分からない          | 外挿が危険                  | 条件変化で急に外れる                           | 必須診断（Cs/Cref、Da、n_app、root反復/失敗率）を標準出力・標準プロット化                            | P0 |
| 連携  | ClearMLを入れたらコアが依存して壊れる         | デプロイ/保守困難              | import error、依存地獄                    | 連携は別パッケージ。coreはイベントフックだけ（logger interface）                                | P1 |

---

# 3. 視点別に深掘り（実務での“痛点”と具体的対応策）

## 3.1 入力・データ整合の課題（CFD×実測×座標）

### 課題1：CFD参照面 z_ref の変更で結果が大きく変わる

* **なぜ起きるか**
  z_refが境界層内なら、Cref自体が表面消費の影響を受け、kmモデルと強く結びつく。
  その結果、同化した次数やkが「z_ref依存の見かけ」になる。

* **対応策（設計）**

  1. z_refは必ずメタデータに残す（run出力に保存）
  2. 参照面の解釈は固定：「バルク濃度」ではなく **参照面濃度**と定義
  3. km側に自由度を持たせる（Ck補正、δ_eff）
  4. **z_ref感度解析を標準レポートに組み込む**

     * 同一条件で z_ref を複数値（例：0.5/1/2mm）で回し、結果の差を可視化
     * 差が大きいなら「kmモデル改善」または「参照面が低すぎ」の判断材料にする

* **対応策（運用）**

  * 代表条件で z_ref を固定し、そのz_refに対してkm/反応定数を校正してから運用する（z_refを変えたら再校正）

---

### 課題2：CFD→ウェハ格子への補間/座標ずれが残差に見える

2D膜厚を合わせるとき、実務で多い失敗がこれです。

* **よくある原因**

  * ウェハ中心のズレ（測定系とシミュレーション座標が一致していない）
  * 回転角のズレ（測定器が装置座標と違う角度基準）
  * エッジ除外領域の扱いが一致していない

* **対応策（必須モジュール）**

  * `MeasurementAdapter` を標準搭載し、以下を “YAMLで選択可能” にする

    * center shift（dx, dy）
    * rotation（θ0）
    * scale（必要なら）
    * mask（edge exclusion、欠損）
  * **同化前に「最良の位置合わせ」を推定**して固定する（もしくは同化の一部として推定する）

---

## 3.2 物理モデル妥当性の課題（回転ディスク前提のズレ）

### 課題3：回転ディスク相関（Levichスケーリング）が装置で合わない

* **原因**

  * 実機は理想的な無限回転円盤ではない（シャワーヘッド供給、排気、温度ムラ、エッジ効果、非対称流れ）
  * よって km の定数がズレたり、r依存/θ依存が出る

* **対応策（仕様として入れるべき）**

  1. kmモデルを単一に固定しない（registryで複数）
  2. Levich型は “理論係数固定” ではなく **補正係数 Ck を同化可能**にする
  3. 必要なら Ck を定数→**低次基底（半径方向多項式/スプライン）**へ拡張できる設計にする

     * Ck(r)=a0+a1 r+a2 r^2 …
     * これが「装置固有の流れの癖」を吸収する最小の自由度になる

* **実務上の重要ポイント**

  * ここで自由度を持たせないと、後段で反応次数や阻害係数が “kmの誤差を肩代わり” してしまい、外挿が壊れます。

---

## 3.3 反応次数・阻害・飽和の課題（同定性・外挿性）

### 課題4：次数 n と速度係数 k のトレードオフで同化が不安定

* **原因**

  * 膜厚マップだけだと、kを上げてnを下げても似た分布になる等、相互補償が起きる
  * 生成物阻害や飽和を無視すると “次数” がそれを吸収して漂流する

* **対応策（モデル戦略）**

  1. 段階導入（MVP順）

     * TC + powerlaw（係数のみ）
     * 次に saturation/inhibition（分母形）
     * 最後に次数推定（拘束つき）
  2. **次数は最初から自由にしない**

     * 候補値（0.5,1,2…）を比較するモデル選択モードを用意
  3. 反応次数は範囲拘束（例：[-2,4]）＋正則化を標準
  4. **見かけ次数マップ n_app を必ず出力**し、物理的に妥当な分布かレビューできるようにする

---

### 課題5：A/Bの“まとめ種”の定義が条件で破綻する

* **原因**

  * A=ラジカル+分子の混合、B=共反応物…のようにまとめると、条件で混合比が変わり得る
  * その変化を無視すると、同化パラメータが条件依存に漂流する

* **対応策（仕様）**

  * まずはA/Bを固定（運用ルール）
  * 必要になったら「有効濃度重み」 (C_A^\mathrm{eff}=\sum w_i C_i) を導入し、w_iを同化/回帰で推定できるようにする

    * これは “機構の全解明” ではなく **まとめ方の柔軟化**として現実的

---

## 3.4 数値計算の課題（収束性・単根性・硬さ）

### 課題6：負次数・強い分母・複数チャネルで F(R) が単調でなくなる

あなたの設計の強みは「1変数Rのbracketing」ですが、**モデルが非単調になると前提が崩れます**。

* **典型例**

  * r(C) が局所的に増えてから減る（阻害が強い、負次数、dep−etch相互作用）
  * すると F(R)=R−r(C(R)) が複数根を持ち得る

* **対応策（必須：数値健全性モジュール）**

  1. **単調性チェック（cheap test）**

     * Rを数点サンプルしてF(R)の形状を判定
     * 怪しいケースはログしてフォールバック
  2. フォールバック階層

     * (a) 区間分割して根を探索（最も小さい物理的根を採用等、ルール化）
     * (b) R以外の未知も持つ（多変数 solve）
     * (c) モデルを制約（パラメータ範囲を狭める、阻害項の形を変更）
  3. “単根であることが保証される形” を推奨モデルとして明示

     * 例えば、反応が Cs に対して単調増加・飽和（分母形でも単調）で、Cs(R) が単調減少なら、Fは単調になりやすい
     * そのための設計ガイドラインをドキュメント化

---

### 課題7：同化時に clip が入ると勾配が壊れる

* **原因**
  非負性を clip で守ると、クリップ領域で勾配がゼロ・不連続になる

* **対策**

  * 同化エンジン（JAX）では

    * `softplus`（非負性）
    * `sigmoid`（0〜1）
      を **YAMLで選択**できるようにする（forwardはclipでも可）
  * forwardの結果と同化の結果が乖離しないよう、「拘束モード」を明示する

---

### 課題8：ALD/改質のODEが stiff になり、dt調整やsolver選択が必要

* **対策**

  * 解析更新できる場合は解析更新を第一選択
  * それが無理なら implicit solver（BDF/Radau）
  * 状態変数は最小（まずは1〜2個）に保つ
  * “ODEが硬いかどうか” を診断（step数、収束警告）してレポートに出す

---

## 3.5 計算資源（CPU/GPU）の課題：小点数・多DOEという現実

### 課題9：単発はGPUが遅い（JIT/転送コスト）

* **対策**

  * `run_strategy=auto` で自動判定

    * 例えば `n_case * n_point * n_iter_root` が小さいならCPU
  * JAXを使う場合でも、単発はCPU JAX、DOEはGPU JAXなど切替を許容

### 課題10：GPUでfloat64が遅い、またはGPU/OS依存がある

* **対策**

  * precisionはYAMLで切替（float32/float64）
  * 検証用に「float32 vs float64の差」を自動比較するモードを用意
  * 必要なら、同化や根探索など “敏感な部分だけ64bit” に寄せる段階的戦略

---

## 3.6 DOE・出力の課題：迷子・肥大・再現性

### 課題11：DOE1000ケースでファイルが爆発する

* **対策（仕様固定が望ましい）**

  * ケースごとにディレクトリを掘らない
  * `zarr` に case 次元で集約（thickness[case, r, theta]）
  * `index.html` に “入口を1つ”＋検索・フィルタ
  * summary.json に主要指標（uniformity、center-edge等）だけ集約

### 課題12：Hydra configが増殖して迷子になる

* **対策**

  * Structured Config（型）を導入して必須項目を固定
  * config groupを最小化（domain/time/engine/models/solvers/report程度）
  * モデル切替は `name:` で行い、ファイル数増殖を抑える
  * resolved configはrunごとに1つだけ保存（run外に散らばらせない）

---

# 4. 実務運用で “成功率を上げる” 追加提案（仕様に入れる価値が高い）

ここからは、上の課題を踏まえて「基盤に最初から入れておくと実務で効く」提案です。

## 4.1 “入力プレビュー” を必須成果物にする

* drivers適用後の Cref, U, T を可視化して保存
* z_ref メタデータとともに保存
  → 「入力が合っているか」が最初のデバッグポイントです。

## 4.2 “数値健全性メトリクス” を標準で出す

* root 反復回数の統計（平均/最大）
* root失敗率
* Csが0に張り付く面積率（輸送律速の兆候）
* 物理拘束違反（θ範囲外、Cs負、等）のカウント

これがあると、「当たり外れ」ではなく「モデルが今どのレジームで壊れているか」が見えます。

## 4.3 モデル比較（Model selection）を “実行器に組み込む”

同化をやると、モデルを一つに固定したくなりますが、実務ではむしろ

* powerlaw
* saturation/inhibition
* competition（LHHW）
  のどれが妥当かを比較しながら進める方が外挿が壊れません。

→ `run_model_compare(cfg, candidates=[...])` のような実行器を用意し、同じデータに対して

* 誤差
* 物理診断（n_app, Cs/Cref, Da）
  を比較できるようにするのが効果的です。

## 4.4 「回転ディスク寄り」だからこそ、軸対称化の診断を入れる

* 入力のθ方向変動（std over θ）
* 出力のθ方向変動
* 実測のθ方向変動

これらを比較し、軸対称近似（1D radial）で十分かどうかを判断できるようにする。
→ DOEの高速化（1D化）は実務価値が大きいので、診断が重要です。

---

# 5. “同じ目的” を保ったまま、実務課題に強い運用フロー（推奨）

最後に、実務導入で詰まりにくい手順を提示します。

## フェーズ1：CVD steady（核の安定化）

* 2D polarで forward
* TC + (powerlaw or saturation)
* kmは停滞膜＋回転ディスク相関（Ck同定可能）
* 物理診断を必ず出す（Cs/Cref、Da、n_app）

## フェーズ2：z_ref感度とkm校正

* z_refを2〜3値で回す（内部検証DOE）
* 出力差が大きい場合はkmモデル改善（Ckをr依存にする等）

## フェーズ3：DOE（10〜1000）

* run_strategy=autoでCPU並列/GPUバッチを切替
* 出力はcase次元zarrで集約

## フェーズ4：同化（opt）

* まず係数のみ → 次に阻害/飽和 → 最後に次数
* 位置合わせ（MeasurementAdapter）とマスク処理を固定してから同化

## フェーズ5：ALD phase + state

* phaseの入力整備（exposure/purge）
* 被覆率stateを最小で導入（解析更新優先）
* stiffになったらimplicit solverへ

---





以下は、これまで提示した **全体仕様（TCRSK：輸送–反応結合＋縮約表面反応運動論＋多次数）**を、実務利用で出る課題への対策を織り込んで **“仕様書 v2（課題対策統合版）”**として再提示したものです。
ご要望どおり **情報量を落とさず、むしろ増やし**、第三者（物理学者／半導体製造装置エンジニア／数値計算専門家）が読んで背景・目的・手法・実装・運用まで追える粒度にしています。

また、注意点2つは仕様に反映しています：

* **計算リソース（CPU/GPU）はユーザーが選ぶ**（用途で決め打ちしない）。`run_strategy=auto` は *任意の補助機能* として扱い、デフォルトで強制しません。
* 「回転ディスク寄り」でも、**回転しない場合（ω=0）も計算できます**。回転は km（質量移動係数）モデルや平均化機能に影響するだけで、基盤（TC＋反応）は回転なしでも成立します。ω=0のときに回転ディスク相関を選んだ場合は、**エラー/警告＋適切なkmモデルへ切替**できるようにします。

---

# 全体仕様書 v2（課題対策統合版）

## Semiconductor Deposition Surface Modeling Platform（CVD/ALD対応）

---

## 1. 背景と目的

### 1.1 背景（現場の制約）

* 装置内CFDで計算できるのは、通常 **ガス流れ・輸送**（ウェハ直上の濃度 (C_\mathrm{ref})、速度 (|u|)、温度 (T) など）まで。
* **膜形成**（表面反応、吸着/脱離、競合、阻害、表面改質、核生成、プラズマ損失等）を厳密に再現しようとすると、

  * 厳密な気体組成
  * 多数の表面素反応（microkinetics）
  * サイト状態、被覆率、生成物再吸着
  * 反応次数の条件依存
    が必要で、実務上コスト/情報が足りず現実的でない。

### 1.2 目的（この基盤が提供する価値）

この基盤は「CFD直上情報 → 膜厚2Dマップ」を **物理妥当性と実務性のバランスを取りながら**実現し、次を可能にする：

1. **CVD（連続）**：定常/過渡の成膜速度から膜厚2D分布を推定
2. **ALD（phase）**：phase（exposure/purge）と状態変数（被覆率等）を含む膜厚推定
3. **多次数反応**（正/負/分数、見かけ次数遷移）を、縮約モデル内で一貫して扱う
4. **DOE 10〜1000条件**を現実的な計算時間と出力管理で回す
5. 将来、**データ同化（パラメータ推定）／最適化／機械学習**へ安全に接続
6. **CPU/GPU両対応**（ただし使用はユーザー選択）
7. YAML（Hydra）で、**ドメイン・モデル・数値解法・計算資源**を全て選択できる

---

## 2. 重要な設計原理（物理・数値・運用のガードレール）

実務で壊れやすい点を踏まえ、設計原理として「最初から仕様に固定」します。

### 2.1 参照面濃度 (C_\mathrm{ref}) は“表面濃度ではない”

* CFDの固定高さ面（z_ref）で得る濃度を **参照面濃度 (C_\mathrm{ref})** と定義し、
* 表面反応に使うのは **表面近傍濃度 (C_s)** とする。
* (C_\mathrm{ref}\to C_s) の橋渡しは **輸送–反応結合（TC）**で行い、ここを省略しない。

### 2.2 反応次数は「後段のML」ではなく「速度式（RateLaw）に含める」

* パワー則（明示指数）
* 飽和/阻害（分母形）
* 状態変数（被覆率・サイト密度）
  の3経路で次数影響を表現し、見かけ次数（条件で変わる次数）も診断可能にする。

### 2.3 数値は「必ず収束する」ルートを標準とする

* TC結合は **反応進行度 (R)** の1変数rootへ落とし、
* bracketing（bisection）を標準にして収束性を確保。
* ただし、負次数/強阻害/多チャネルで単調性が崩れる可能性があるため、**単調性チェック＋フォールバック**を仕様として持つ。

### 2.4 出力は「迷子防止」を最優先に設計

* `results/index.html` を固定入口（常にここを見れば良い）
* DOEでもケースごとディレクトリを掘らず、**case次元を持つ配列ストア**（zarr等）に集約

### 2.5 計算資源（CPU/GPU）はユーザーが選ぶ

* GPUが有効な場合はあるが、環境・設定・規模で逆効果もあり得る。
* したがって、**デフォルトでCPU/GPUを決め打ちしない**。
* `run_strategy=auto` は *補助* とし、ユーザー指定を尊重する。

---

## 3. 全体アーキテクチャ（パッケージ分離・疎結合）

### 3.1 リポジトリ構成（ファイル増殖を抑え、拡張しやすい）

```
repo/
  pyproject.toml
  README.md

  deposim_schema/               # 共通データ契約（型・単位・Config構造）
  deposim_sim/                  # 数値計算（forward/DOE、堅牢）
  deposim_opt/                  # 同化/最適化（可微分計算、JAX系）
  deposim_report/               # 可視化・レポート・プロットレジストリ
  deposim_tracking_clearml/     # ClearML連携（任意、core非依存）

  configs/
    sim/
    opt/

  examples/
    cvd_forward.ipynb
    cvd_doe.ipynb
    ald_phase.ipynb
    assimilation.ipynb
```

### 3.2 依存関係の方針（重要）

* `deposim_sim`：NumPy/SciPy中心。JAXは optionalでも良い（環境依存があるため）。
* `deposim_opt`：JAX/最適化（LBFGS等）を前提にしつつ、coreに依存させない。
* `deposim_tracking_clearml`：**完全分離**。coreはClearMLをimportしない。

---

## 4. データ契約（schema）：第三者が迷わない I/O の固定

ここが曖昧だと、実務で “毎回入力形式が違う” になり破綻します。

### 4.1 必須Spec

* `DomainSpec`

  * `type`: `wafer_2d_polar | wafer_2d_xy | wafer_1d_radial`
  * grid定義（点数、半径、角度）
  * mask（edge exclusion, valid region）
  * 観測座標→計算座標のマッピング方式
* `ReferencePlaneSpec`

  * `z_ref_mm`（固定面高さ）
  * `mode`: `fixed_plane`（将来 `multi_plane`）
  * `aggregation`: `point | slab_mean`（面内平均方法）
* `TimeSpec`

  * `mode`: `steady | transient | phases`
  * `steady`: `process_time_s`
  * `transient`: `t0,t1,dt`
  * `phases`: `[{name, duration, dt, fields_ref}]`
* `InputsSpec`

  * `fields`: `C_A, C_B, U, T ...`（2Dマップ/1D配列/スカラー）
  * `scalars`: `P, omega, ...`
  * `state_init`: 被覆率等の初期条件（スカラー/マップ/式）
* `DriversSpec`

  * 時空間で外部入力を変更するルール（後述）
* `ModelSpec`

  * `mass_transfer`（kmモデル）
  * `rate_law`（反応速度式族）
  * `state_model`（ALD/改質）
  * `net_model`（dep/etch/lossの合成）
* `SolverSpec`

  * `root`（bisection/brentq/hybrid）
  * `ode`（analytic/explicit/implicit）
* `ComputeSpec`

  * `engine`: `numpy | jax`
  * `device`: `cpu | gpu`（jaxの場合）
  * `precision`: `float32 | float64`
  * `jit`: true/false（jax）
  * `batching`: DOEをバッチ化するか
* `OutputSpec`

  * 生成物（zarr/npz）とレポート（html/plots）の構成

---

## 5. 核となる理論：TCRSK（TC＋縮約反応）

### 5.1 局所モデル（面内各点で解く）

入力（既知）：

* 参照面濃度：(C_{\mathrm{ref},i}(x,t))
* 参照面速度：(|u|(x,t))
* 温度：(T(x,t))
* 追加スカラー：圧力 (P)、回転角速度 (\omega)（回転なしなら0）

未知：

* 表面近傍濃度：(C_{s,i}(x,t))
* 反応速度：(r_j(x,t))
* 状態：(z(x,t))
* 膜厚：(h(x,t))

輸送：
[
J_i = k_{m,i}(x,t)\left(C_{\mathrm{ref},i}-C_{s,i}\right)
]

反応（縮約速度式）：
[
r_j = r_j({C_s}, z, T; \theta)
]

収支：
[
J_i = \sum_j \nu_{ij} r_j
]

状態（必要時）：
[
\frac{dz}{dt} = g({C_s},z,T;\theta)
]

膜厚：
[
\frac{dh}{dt} = \sum_j \alpha_j r_j
]

---

## 6. “回転ディスク寄り”＋“回転しない場合も成立”する物理モデル仕様

ユーザー確認：「回転しない場合も計算できるか？」
→ **できます**。回転は主に km と平均化機能に効くだけで、TCRSK自体は回転なしでも成立します。
実装上は「回転がある/ない」を **入力スカラー (\omega)** で扱い、**kmモデルの選択**で適切に対応します。

### 6.1 ドメイン選択（回転あり/なし共通）

* 実測が2D全面なので、標準は `wafer_2d_polar` または `wafer_2d_xy`
* 回転がないときでも `2d_polar` は使えます（非軸対称の入力も (\theta) 方向に表現可能）
* 軸対称近似が成立する場合のみ `wafer_1d_radial` を高速モードとして用意

### 6.2 回転平均化（オプション）

* `rotation_average = none`（デフォルト）
  → 実測が2Dなのでまずこれ
* 回転が高速で軸対称に近いなら

  * `azimuthal_mean` で入力・出力を1D化（DOEで強力）

回転しない場合（ω=0）：

* `rotation_average` は無効にするか警告（平均化すると物理が変わるため）

---

## 7. km（質量移動係数）モデル：回転あり/なしで選べる仕様

kmは z_ref感度やムラ再現性に直結するので、**必ずレジストリで切替可能**にします。

### 7.1 kmモデル一覧（推奨順と使いどころ）

1. **stagnant_film（有効停滞膜）**
   [
   k_m = \frac{D}{\delta_\mathrm{eff}}
   ]

   * 回転あり/なし両対応
   * (\delta_\mathrm{eff}) を校正できる（z_ref差を吸収しやすい）
   * 最初の本命

2. **rotating_disk（回転ディスク相関：Levichスケール）**
   [
   k_m \sim C_k D^{2/3}\omega^{1/2}\nu^{-1/6}
   ]

   * 回転ありのときに有効
   * **ω=0では不適切**なので、選択時に

     * エラー（strict）
     * または自動フォールバック（fallback_to=stagnant_film）
       をYAMLで選べるようにする

3. **flat_plate / generic_convective（非回転の対流相関）**

   * 回転なし・流れが一方向/代表速度で近似できる場合に有効
   * （厳密相関を固定化しなくても、`Ck`補正で装置差を吸収する設計にできる）

4. **cfd_provided_km（CFD派生 km 入力）**

   * 最も物理寄り
   * CFD側運用コストは高いが、z_refの曖昧さを減らせる

5. **learned_km（経験的モデル）**

   * kmを低次基底（r方向多項式など）＋条件依存で表現し、同化対象にする
   * “反応が良く分からないが、輸送差は吸収したい” ときに有効
   * ただし自由度を上げすぎないよう、正則化を必須にする

### 7.2 km選択の運用ガイド（実務課題対策を仕様化）

* z_refを変えたら km（特に (\delta_\mathrm{eff}) や (C_k)）が再校正されるべき
  → **z_ref感度DOE**を標準レポートにする（後述）
* “回転ディスク相関を使っているのに (C_k) が条件ごとに大きく漂流する” 場合
  → 相関が装置で破れている兆候。`C_k(r)`（低次基底）や `stagnant_film` を検討。

---

## 8. 速度式（RateLaw family）仕様：多次数・遷移・負次数まで一貫

### 8.1 RateLaw I/F（固定）

```python
class RateLaw(Protocol):
    def rate(self, Cs: dict[str, Array], state: State, T: Array, params: Params) -> Array:
        ...

    def apparent_orders(self, Cs, state, T, params) -> dict[str, Array]:
        ...
```

### 8.2 標準搭載モデル（CVD優先＋ALD対応）

* `powerlaw`：一般化パワー則（nは正/負/分数可）
* `saturation_inhibition`：分母形（飽和・阻害・見かけ次数遷移）
* `lhhw_competition`：競合吸着/協働反応の縮約
* `er_like`：ガス相衝突（ラジカル）を反映した縮約
* `ald_sticking`：((1-\theta)^m) 型の自己終端を含む形

### 8.3 実務課題対策としての “診断出力”

* `apparent_orders()` を可能な範囲で実装し、マップとして出力
  [
  n^\mathrm{app}_i(x)=\frac{\partial\ln r}{\partial\ln C_i}
  ]
* 見かけ次数が極端（例：大きな負、領域で不連続）なら、

  * km誤差吸収
  * 阻害項不足
  * モデル単調性破れ
    の疑いとしてレポートに警告を出す。

---

## 9. TC結合の数値解法：1変数root＋単調性チェック＋フォールバック（課題対策を仕様化）

### 9.1 反応進行度 (R) に落とす（標準）

支配反応が1本のとき：

[
C_{s,i}=C_{\mathrm{ref},i}-\frac{\nu_i}{k_{m,i}}R
]
[
F(R)=R-r(C_s(R),z,T)=0
]
[
0\le R\le R_{\max}=\min_i\frac{k_{m,i}C_{\mathrm{ref},i}}{\nu_i}
]

### 9.2 標準ソルバ：配列bisection（CPU/GPU共通）

* bisectionは収束保証・実装が単純・固定反復にしやすい
* GPU/JITにも載せやすい

### 9.3 実務課題対策：単調性チェック（必須）

負次数・強阻害・dep−etchなどで **F(R)が単根でない**可能性があります。
この対策を仕様として組み込みます。

#### 単調性チェックの例（軽量）

* (R) を 0〜Rmax の間で数点サンプルし、

  * 符号変化の回数
  * 増減の一貫性
    を判定する。

#### フォールバック階層（仕様）

1. **区間分割 root**：符号変化が複数ある場合、区間ごとに根を探し、採用ルールを適用

   * 例：物理的に最小の正根を採用、または安定根を採用（要件に合わせて設定）
2. **hybrid**：bisectionで近づけてから固定回数Newton（単調性が保たれる範囲のみ）
3. **multivariate solve**（最終手段）：Rではなく Cs を未知にする多変数ソルバへ

   * ただし実装/頑健性コストが高いので、最終手段として位置づける
4. **モデル/パラメータ制約**：単調性が壊れにくいモデル形へ戻す（運用ルール化）

この仕組みは、実務で「ある条件だけ計算が壊れる」を防ぐために重要です。

---

## 10. 時間ドメイン：CVD優先の設計（steady→transient→phases）

### 10.1 `steady`（CVDの本命、最速・最堅牢）

* 時間発展を解かず、成膜速度 (\dot{h}(x,y)) を計算して
  [
  h = h_0 + \dot{h},t_\mathrm{proc}
  ]
* DOEで1000条件を回す主力モード

### 10.2 `transient`（入力が時間で変わるCVD）

* driversで流量/温度/圧力が時間変化する場合に使用
* 時間刻み dt は YAML
* 収束性のため、`warm_start` を標準有効化できる（R前時刻を初期区間に使う）

### 10.3 `phases`（ALD）

* phaseごとの入力束（Cref, T など）と duration/dt
* state更新を含む（解析更新優先 → 必要ならimplicit ODE）

---

## 11. 外部入力変更（drivers）・初期条件入力（state_init）：実務課題対策込み

### 11.1 DriverSpec（仕様）

* `target`: 例 `inputs.scalars.P_Pa` / `inputs.fields.C_A`
* `when`: timeレンジ or phase
* `where`: mask（edge, center, custom region）
* `op`: replace/add/multiply/clamp
* `source`: constant/table/map/expression

### 11.2 実務課題対策：入力プレビューを必須出力

driversは便利ですが、**誤適用が最頻出事故**です。
したがって、

* drivers適用前後の差分マップ
* 適用順序
* 最終入力の統計
  を必ず保存し、レポートに表示します。

---

## 12. 計算資源（CPU/GPU）と実行戦略：ユーザー選択を尊重した仕様

ご注意の通り、「単発はCPU」「DOEはGPU」など **用途で決め打ちしません**。
ユーザーが環境と目的に応じて選べるようにし、さらに補助として “提案/ベンチ” は用意します。

### 12.1 ComputeSpec（YAML）

* `engine: numpy | jax`
* `device: cpu | gpu`（jaxのみ）
* `precision: float32 | float64`
* `jit: true/false`（jax）
* `batching: none | vmap_cases`（DOEの一括化）

### 12.2 run_strategy（YAML）

* `single`：単一条件
* `sweep_cpu_parallel`：DOEをCPU並列
* `sweep_gpu_batch`：DOEをバッチ化してGPU（jax）
* `auto`：**任意**（ユーザーが選んだときだけ使う）

### 12.3 実務課題対策：ベンチマーク機能（推奨）

“GPUが有用か” はケース依存です。そこで

* `benchmark` モードを提供し、

  * 代表ケースでCPU/JAX‑CPU/JAX‑GPUを比較
  * DOEサイズを変えたときのスループットを測定
  * レポートに「推奨設定（ただし強制しない）」を出す
    という運用にします。

---

## 13. DOE・複数条件実行：出力爆発を防ぐ仕様（課題対策込み）

### 13.1 DOE実行器（API）

* `run_sim(cfg)`：単一
* `run_sweep(cfg, sweep_spec)`：DOE（10〜1000）
* `run_opt(cfg)`：同化（optパッケージ）

### 13.2 出力設計（迷子防止）

```
project/
  results/
    index.html
    summary.json
    runs/
      <run_id>/
        config_resolved.yaml
        inputs_preview/
        outputs/
          thickness.zarr
          diagnostics.zarr
        plots/
        report.html
```

DOEのとき：

* `thickness.zarr` は `case` 次元を持ち、ケースを集約（ケースごとにディレクトリを掘らない）
* `summary.json` に各ケースの主要指標（uniformity、center-edge、最大/最小など）を格納
* `report.html` で DOEのPareto、感度（Sobol/回帰/SHAP等は将来拡張）を表示

---

## 14. 可視化・診断（同化・レビュー・外挿安全性のための必須セット）

「当たったが理由が分からない」を防ぐため、物理診断を標準で出します。

### 14.1 必須プロット

* 膜厚2Dマップ（最終）
* 半径プロファイル（中心→エッジ）＋θ方向ばらつき
* 入力（Cref, U, T）マップ
* drivers差分マップ

### 14.2 物理診断（必須）

* (C_s/C_\mathrm{ref}) マップ：枯渇/輸送律速の兆候
* Da proxy：反応/輸送の相対指標（モデル内部から算出）
* 見かけ次数 (n_\mathrm{app}) マップ：次数の妥当性チェック
* root反復回数マップ／失敗率／フォールバック発生率（数値健全性）

---

## 15. データ同化・最適化・ML接続（目的は同じ、将来に繋がる設計）

### 15.1 同化で詰まりやすい点への対策（仕様）

* 非負性や範囲拘束を hard clip すると勾配が壊れる場合がある
  → opt側では `softplus/sigmoid` 等の滑らか拘束を選べる
* パラメータは変換を標準化

  * 正値：`k=exp(κ)`
  * 次数：範囲拘束＋正則化
* モデル自由度は段階導入（powerlaw→分母形→次数推定→state）

### 15.2 モデル比較（実務対策として推奨）

* 同一データに対して `powerlaw` と `saturation_inhibition` と `lhhw_competition` を比較し、

  * 誤差
  * 物理診断（n_app, Cs/Cref, Da）
    を同時に見られる「model_compare」実行器を用意する。
    これにより、外挿で壊れにくいモデルを選びやすい。

---

## 16. 実務課題への対策を仕様として明文化（FMEA的まとめ）

以下は「実務で起きやすい課題」と「仕様での対策」を、**実装要件として固定**した一覧です（情報量を増やして再整理）。

### 16.1 参照面（z_ref）課題への対策

* **仕様**：z_refはメタデータとして保存し、参照面濃度として扱う
* **標準機能**：z_ref感度解析（DOE）

  * 同一条件で z_ref を複数値に変えて膜厚差・診断差を出す
* **運用**：z_refを変えたら km/反応パラメータの再校正が必要（ガイドライン化）

### 16.2 回転あり/なし課題への対策（今回の確認事項）

* **仕様**：回転は (\omega) を入力するだけ。TCRSK計算自体は ω=0でも成立。
* **kmモデルのガード**

  * `rotating_disk` を選んで ω=0 の場合：

    * `strict=true`：エラーで止める（安全）
    * `fallback_to=stagnant_film`：警告して自動切替（運用重視）
  * どちらも YAML で選択可
* **平均化機能**

  * ω=0 の場合 `rotation_average` は警告または無効化（選択可）

### 16.3 同定性（次数/阻害/飽和）課題への対策

* **仕様**：分母形（飽和/阻害）を最初から選べる
* **運用**：次数推定は段階導入（自由度を一気に増やさない）
* **診断**：見かけ次数マップを標準出力

### 16.4 数値（多根・硬いODE）課題への対策

* **仕様**：単調性チェック＋フォールバック階層を標準搭載
* **仕様**：ALD stateは解析更新優先、必要ならimplicit ODEへ
* **診断**：失敗率・フォールバック率・stiff指標（step爆増など）を出力

### 16.5 出力迷子・容量爆発への対策

* **仕様**：`results/index.html` 固定入口
* **仕様**：DOEは case 次元に集約（ディレクトリを掘らない）
* **仕様**：resolved config は runディレクトリに1つ

### 16.6 CPU/GPUの使い分け課題への対策（ユーザー選択尊重）

* **仕様**：ユーザーが `engine/device` をYAMLで選ぶ
* **補助**：benchmarkモードで推奨を出す（強制しない）
* **補助**：`run_strategy=auto` は任意

---

## 17. Hydra/YAML運用（増殖を防ぎつつ全選択可能）

### 17.1 運用ルール（事故防止）

* config group を増やしすぎない（domain/time/models/solvers/compute/report程度）
* モデルの切替は `name:` と `params:` で行い、ファイル増殖を避ける
* 実行ごとの resolved config を保存して再現性を担保

---

## 18. YAML例（回転あり・回転なし・DOE・ALD）※ユーザー選択型

### 18.1 回転あり（CVD steady）

```yaml
compute:
  engine: numpy
  device: cpu
  precision: float64

reference_plane:
  z_ref_mm: 1.0

inputs:
  scalars:
    omega_rad_s: 200.0

models:
  mass_transfer:
    name: rotating_disk
    params:
      Ck: 0.62
      D_m2_s: 1.0e-4
      nu_m2_s: 1.5e-5
    guard:
      omega_zero: error
```

### 18.2 回転なし（ω=0でも計算可能：停滞膜km）

```yaml
compute:
  engine: numpy
  device: cpu

inputs:
  scalars:
    omega_rad_s: 0.0

models:
  mass_transfer:
    name: stagnant_film
    params:
      D_m2_s: 1.0e-4
      delta_eff_m: 0.002
```

### 18.3 回転なしだが rotating_disk を選んでしまった場合（フォールバック）

```yaml
models:
  mass_transfer:
    name: rotating_disk
    params: {Ck: 0.62, D_m2_s: 1.0e-4, nu_m2_s: 1.5e-5}
    guard:
      omega_zero: fallback
      fallback_to: stagnant_film
      fallback_params:
        delta_eff_m: 0.002
```

### 18.4 DOE（ユーザーがGPUを選ぶ例）

```yaml
compute:
  engine: jax
  device: gpu
  precision: float64
  jit: true
  batching: vmap_cases

run:
  strategy: sweep_gpu_batch

sweep:
  mode: grid
  parameters:
    inputs.scalars.P_Pa: [200, 400, 600]
    inputs.scalars.omega_rad_s: [0, 100, 200]
```

> ※ここでも「DOEはGPU」と決め打ちではなく、ユーザーが `engine/device/strategy` を選びます。

---

## 19. 実務導入の推奨フロー（課題対策込み）

1. **CVD steady（2D）で核を固める**

   * TC + powerlaw/saturation
   * kmは停滞膜 or 回転ディスク（Ck同定可能）
   * 必須診断を出す

2. **z_ref感度解析を標準で実施**

   * z_refを複数値で回し、結果が大きく変わるなら km側の改善を優先

3. **DOE（10〜1000）へ拡張**

   * ユーザーが CPU並列 or GPUバッチを選択
   * 出力はcase次元集約

4. **同化（opt）**

   * 係数のみ→分母形→次数推定→state の段階導入
   * 2D観測の位置合わせを固定化（MeasurementAdapter）

5. **ALD phases + state**

   * phase入力・被覆率state導入
   * stiff対策（解析更新→implicit）

---

# 20. 最終回答（確認事項への回答）

### Q1：回転しない場合でも計算できますか？

**計算できます。**
回転は（主に）`mass_transfer`（kmモデル）と `rotation_average`（平均化）に影響するだけで、**TCRSK（TC＋反応）自体は ω=0 でも成立**します。
仕様として、ω=0 のときに回転ディスク相関を選んだ場合は **エラー/フォールバック**をYAMLで選べるようにしてあります。

### Q2：計算リソースはユーザーが選ぶ前提でよいか？

**その前提で仕様に固定しました。**
`run_strategy=auto` は補助機能として残しますが、デフォルトで強制はせず、ユーザーが `engine/device/strategy` をYAMLで指定して運用できる設計にしています。加えて、判断材料として `benchmark` モードを推奨機能として提案しています（強制しない）。

---

## 次に（必要なら）

この仕様書をそのまま実装へ落とすために、次の成果物を追加で作れます（情報量はさらに増えます）：

* **Structured Config（dataclass）定義の完全版**（sim/opt分割）
* **モデルレジストリ＋エンジン抽象＋ソルバ（単調性チェック/フォールバック含む）**のコード骨格
* **レポート/診断のテンプレ**（index.html固定入口、DOE集約対応）

必要なら「最小ファイル数」で作る方針で提示します。
